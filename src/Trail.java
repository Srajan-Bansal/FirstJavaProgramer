import Kunal_Kushwaha.LinkedList.Questions.LinkedListLeetcode;import java.util.Arrays;import java.util.ArrayList;import java.util.List;public class Trail implements LinkedListLeetcode {    public static void main(String[] args) {//        System.out.println('A' - 'a');        System.out.println(letterCasePermutation("a1b2"));    }    public static List<String> letterCasePermutation(String s) {        List<String> list = new ArrayList<>();        permutation("", s, list);        return list;    }    static void permutation(String p, String up, List<String> list) {        if (up.isEmpty()) {            list.add(p);            return;        }        char ch = up.charAt(0);        if (ch >= '1' && ch <= '9') {            permutation(p + ch, up.substring(2), list);        } else {            if (ch >= 'A' && ch <= 'Z') {                ch -= 32;            } else ch += 32;            permutation(p + ch, up.substring(1), list);        }    }}